import React from 'react';
import Gallery from "../../Gallery/Gallery/Gallery";

const filteredCollection = () => {
    return (
        <Gallery url="/movies/filter"/>
    );
};

export default filteredCollection;